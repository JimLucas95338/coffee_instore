import BrandClient from './BrandClient';

export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'Brand & Theme — Admin',
};

export default function BrandAdminPage() {
  return <BrandClient />;
}
